# Optional dependencies for extended solution analysis.
# Core analysis works without any of these — install for deeper insight.
#
# Usage:
#   pip3 install -r agent/requirements-analyze.txt
#
# Each library is auto-detected at runtime. Missing libraries skip their
# features gracefully — see: python3 agent/scripts/analyze.py --list-extensions

networkx>=3.0       # Graph topology, community detection, cycle detection
pandas>=2.0         # Statistical profiling, outlier detection
matplotlib>=3.7     # Visualizations (heatmaps, charts, graph diagrams)
jinja2>=3.1         # Rich templated reports
